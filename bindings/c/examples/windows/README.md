# AccessKit Windows example

This example demonstrates how to integrate AccessKit into a C/C++ project using CMake.

## Building

```bash
cmake -S . -B build -DACCESSKIT_DIR="../.."
cmake --build build --config Release
```
