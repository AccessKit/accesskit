[package]
name = "accesskit_c"
version = "0.1.0"
edition = "2021"

[lib]
name = "accesskit"
crate-type = ["cdylib", "staticlib"]

[dependencies]
accesskit = { version = "0.10.1", path = "../../common" }
paste = "1.0"
