[workspace]
resolver = "2"
members = [
    "common",
    "consumer",
    "platforms/macos",
    "platforms/unix",
    "platforms/web",
    "platforms/web/run-wasm",
    "platforms/windows",
    "platforms/winit",
    "bindings/c",
]
default-members = [
    "common",
    "consumer",
    "platforms/web",
    "platforms/winit",
    "bindings/c",
]

[profile.release]
lto = true
panic = "abort"
debug = true
opt-level = "z"
codegen-units = 1
