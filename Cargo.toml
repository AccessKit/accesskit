[workspace]
members = [
    "schema",
    "consumer",
    "provider",
    "platforms/mac",
    "platforms/mac/jni",
    "platforms/windows",
]
default-members = [
    "schema",
    "consumer",
    "provider",
]

[profile.release]
lto = true
