[workspace]
members = [
    "common",
    "consumer",
    "platforms/mac",
    "platforms/mac/jni",
    "platforms/windows",
]
default-members = [
    "common",
    "consumer",
]

[profile.release]
lto = true
