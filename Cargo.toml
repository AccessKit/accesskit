[workspace]
members = [
    "schema",
    "consumer",
    "platforms/mac",
    "platforms/mac/jni",
    "platforms/windows",
    "platforms/windows/bindings",
]
default-members = [
    "schema",
    "consumer",
]

[profile.release]
lto = true
