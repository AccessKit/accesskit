[workspace]
members = [
    "common",
    "consumer",
    "platforms/mac",
    "platforms/windows",
    "platforms/winit",
]
default-members = [
    "common",
    "consumer",
    "platforms/mac",
    "platforms/winit",
]

[profile.release]
lto = true
