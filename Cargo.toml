[workspace]
members = [
    "schema",
    "consumer",
    "platforms/mac",
    "platforms/mac/jni",
    "platforms/windows",
]
default-members = [
    "schema",
    "consumer",
]

[profile.release]
lto = true
