[workspace]
resolver = "2"
members = [
    "common",
    "consumer",
    "platforms/macos",
    "platforms/unix",
    "platforms/windows",
    "platforms/winit",
    "bindings/c",
    "bindings/python",
]
default-members = [
    "common",
    "consumer",
    "platforms/winit",
    "bindings/c",
    "bindings/python",
]

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
