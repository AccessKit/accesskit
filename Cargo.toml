[workspace]
members = [
    "common",
    "consumer",
    "platforms/macos",
    "platforms/unix",
    "platforms/windows",
    "platforms/winit",
    "bindings/c",
]
default-members = [
    "common",
    "consumer",
    "platforms/winit",
]

[profile.release]
lto = true
