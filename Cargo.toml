[workspace]
members = [
    "common",
    "consumer",
    "platforms/windows",
    "platforms/winit",
]
default-members = [
    "common",
    "consumer",
    "platforms/winit",
]

[profile.release]
lto = true
