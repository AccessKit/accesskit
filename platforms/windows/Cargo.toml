[package]
name = "accesskit_windows"
version = "0.1.0"
edition = "2018"

[dependencies]
accesskit_consumer = { path = "../../consumer" }
accesskit_schema = { path = "../../schema" }
accesskit_windows_bindings = { path = "bindings" }
windows = "0.21.0"
